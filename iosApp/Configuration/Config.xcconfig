TEAM_ID=UDTBP44Q7F
APP_BUNDLE_IDENTIFIER=info.jukov.player.Jukovplayer
APP_DISPLAY_NAME=the only player
// Developers from another Apple team can override TEAM_ID and
// APP_BUNDLE_IDENTIFIER in the gitignored Local.xcconfig. Firebase Crashlytics is
// enabled only for the registered default bundle identifier.
#include? "Local.xcconfig"

PRODUCT_NAME=Jukovplayer
PRODUCT_BUNDLE_IDENTIFIER=$(APP_BUNDLE_IDENTIFIER)

CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0
